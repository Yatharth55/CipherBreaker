#Cipher_Breaker
cipher breaker is an machine learning model that scrapes sentences from a book and encrypt it with random keys,
and then performs various tests and store all that information in json format
and then we train our model on basis of that data alone
